<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Culture 974{% endblock %}</title>

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🌋</text></svg>">

        {# Intégration de Google Fonts pour un look plus moderne #}
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

        <style>
            body { font-family: 'Poppins', sans-serif; background-color: #f8f9fa; }
            .navbar-brand { font-weight: 600; color: #d32f2f !important; }
            .card { transition: transform 0.2s; border: none; }
            .card:hover { transform: translateY(-5px); }
        </style>

        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {# Barre de Navigation #}
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_event_index') }}">🌋 Culture 974</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_event_index') }}">Événements</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link btn btn-outline-light btn-sm ms-lg-2" href="{{ path('app_event_new') }}">Publier</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        {# Contenu principal #}
        <main class="container">
            {% block body %}{% endblock %}
        </main>

        {# Footer #}
        <footer class="text-center py-4 mt-5 border-top text-muted">
            <p>&copy; {{ "now"|date("Y") }} - Culture 974 : La Réunion en mouvement</p>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </body>
</html>